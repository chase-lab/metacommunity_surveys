source("renv/activate.R")

if (Sys.info()["user"] == "as80fywe") data.table::setDTthreads(8) # this sets (and in this case increases) the number of cores data.table can use for its parallelised operations such as reading or writing files and many other functionalities.


base::options(prompt = "R> ", digits = 4, show.signif.stars = FALSE, continue = "      ")


message("*** Successfully loaded Metacommunity surveys Project .Rprofile ***")
